# BrainRot CLI v2 - OpenTUI Rewrite Progress

## Completed Tasks

### Phase 1: Core Setup

- [x] **Task 1.1: Set up new project with OpenTUI dependencies**
  - Added `@opentui/core@^0.1.72` and `@opentui/react@^0.1.72` to package.json
  - Updated React to `^19.0.0` (compatible with OpenTUI)
  - Created new source directory structure: `src-opentui/`
    - `src-opentui/index.tsx` - Entry point with createCliRenderer + createRoot
    - `src-opentui/App.tsx` - Placeholder App component
    - `src-opentui/components/` - UI components directory
    - `src-opentui/claude/` - Claude integration directory
    - `src-opentui/games/` - Games directory
    - `src-opentui/hooks/` - React hooks directory
    - `src-opentui/config/` - Configuration directory
    - `src-opentui/themes/` - Theme system directory
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 1.2: Create basic render loop and App shell**
  - Implemented application state machine with all states: INIT, PRD_INPUT, LOOP_RUNNING, PAUSED, COMPLETE, ERROR
  - Added `useTerminalDimensions` hook for responsive layout
  - Created split-pane layout structure with Claude Panel and Game Panel placeholders
  - Implemented Tab key focus switching between panels (visual feedback with border style)
  - Added status bar showing: app state, focus target, terminal dimensions, key hints
  - Added Ctrl+N to cycle through states (for testing state machine)
  - Added Ctrl+E to toggle error state (for testing)
  - Exported TypeScript types: `AppState`, `FocusTarget`, `AppContext`
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

### Phase 2: Layout System

- [x] **Task 2.1: Build split-pane with flexbox**
  - Created `src-opentui/components/Layout.tsx` - Main layout component with split-pane structure
    - Flexbox-based layout with configurable split ratio (default 60/40)
    - Alt+Left/Right keyboard shortcuts to resize panels
    - H key to toggle panel visibility based on focus
    - Alt+H to reset layout to defaults
    - Min/max panel width constraints (20-80%)
  - Created `src-opentui/components/ClaudePanel.tsx` - Claude output panel
    - Border style changes based on focus state (double when focused)
    - Placeholder for scrollable output area
    - Error display area with visual styling
    - Input hint when focused
  - Created `src-opentui/components/GamePanel.tsx` - Game container panel
    - Border style changes based on focus state
    - Game selection prompt when no game active
    - Score display header when game is active
    - Shows available games list
  - Created `src-opentui/components/StatusBar.tsx` - Bottom status bar
    - Loop status: iteration count, current task
    - Game info: name and score
    - Focus indicator with direction arrows
    - Terminal dimensions display
    - Key hints for common actions
  - Created `src-opentui/components/index.ts` - Component exports
  - Updated `src-opentui/App.tsx` to use new component architecture
    - Refactored to use Layout, ClaudePanel, GamePanel, StatusBar components
    - STATE_INFO now a constant object for cleaner code
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

### Phase 3: Claude Integration

- [x] **Task 3.1: Port process management**
  - Created `src-opentui/claude/process.ts` - ClaudeProcess class
    - Event-based child process management with EventEmitter
    - Spawn options: executablePath, args, cwd, env, shutdownTimeout
    - Process status tracking: idle, starting, running, stopping, stopped, crashed
    - Graceful shutdown with SIGTERM → timeout → SIGKILL fallback
    - Type-safe event listeners: onStdout, onStderr, onStatus, onError, onExit
    - stdin write/writeLine methods for input
  - Created `src-opentui/claude/index.ts` - Module exports
  - Created `src-opentui/hooks/useClaudeProcess.ts` - React hook
    - Manages ClaudeProcess lifecycle within React component
    - Output collection with configurable max lines (default 1000)
    - Automatic cleanup on unmount
    - Exposes: spawn, stop, kill, write, writeLine, clearOutput
    - Returns: status, output, error, isRunning
  - Created `src-opentui/hooks/index.ts` - Hook exports
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 3.2: Implement simplified Ralph loop**
  - Created `src-opentui/claude/ralph-loop.ts` - RalphLoop class
    - Event-based orchestration with EventEmitter
    - PRD loading and validation from prd.json files
    - PRD document structure: title, tasks, currentTask, iteration, maxIterations
    - Task structure: id, title, description, status (pending/in_progress/completed), iteration
    - Loop status tracking: idle, loading_prd, running, waiting_for_input, paused, complete, error
    - Iteration management with configurable max iterations
    - Prompt building for Claude based on PRD state
    - Automatic PRD saving after each iteration
    - Simple task completion detection from Claude output
    - Type-safe event listeners: onStatus, onPRDLoaded, onIterationStart, onIterationComplete, onTaskUpdated, onOutput, onError, onComplete
    - Control methods: runIteration, runLoop, pause, resume, stop, kill, sendInput, updateTask
  - Created `src-opentui/hooks/useRalphLoop.ts` - React hook
    - Manages RalphLoop lifecycle within React component
    - State tracking: status, prd, iteration, maxIterations, currentTask, completedTasks, pendingTasks
    - Output collection with configurable max lines (default 1000)
    - Progress calculation (0-100%)
    - Automatic cleanup on unmount
    - Optional auto-start after PRD loading
    - Exposes: loadPRD, runIteration, runLoop, pause, resume, stop, kill, sendInput, updateTask, clearOutput
    - Returns: status, prd, iteration, maxIterations, currentTask, completedTasks, pendingTasks, output, error, progress, isRunning, isComplete
  - Updated `src-opentui/claude/index.ts` - Added Ralph loop exports
  - Updated `src-opentui/hooks/index.ts` - Added useRalphLoop exports
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

### Phase 4: Games

- [x] **Task 4.1: Port game logic (unchanged)**
  - Created `src-opentui/game-types.ts` - Game framework types and interfaces
    - GameStatus, GameInput, GameDimensions, BaseGameState types
    - GameInfo, LoopAttention, GameStateUpdate, GameComponentProps interfaces
    - GameRegistryEntry, GameLoopInfo, CanvasCell types
    - Point, Vector2D, Direction types with utility functions
    - OpenTUIKeyEvent interface for OpenTUI keyboard handling
    - createGameInput() function adapted for OpenTUI events
    - Helper functions: directionToVector, pointsEqual, addVectors
  - Created `src-opentui/hooks/useGameLoop.ts` - Frame-based game loop hook
    - Configurable target FPS (default 30)
    - Rolling FPS average calculation (last 10 frames)
    - Provides deltaTime, elapsedTime, frameCount
    - start/stop/toggle/reset controls
    - Automatic start/stop based on isActive prop
    - Cleanup on unmount
  - Created `src-opentui/games/index.ts` - Game registry
    - gameRegistry array for game components
    - getGameList() - returns all game info
    - getGameById(id) - lookup by game ID
    - hasGame(id) - check if game exists
    - getGameCount() - total games available
  - Updated `src-opentui/hooks/index.ts` - Added useGameLoop exports
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 4.2: Adapt rendering to OpenTUI components (Snake)**
  - Created `src-opentui/games/SnakeGame.tsx` - Snake game for OpenTUI
    - Full game logic preserved from original (direction queue, collision detection, speed scaling)
    - Rendering adapted to use OpenTUI `<box>` and `<text>` components
    - 2D character board rendering with colored elements (snake head, body, food, border)
    - useKeyboard hook for input handling (Arrow keys, WASD, P/R/Q controls)
    - useGameLoop for frame-based timing with delta-time movement
    - HUD showing score, high score, and FPS
    - Game states: playing, paused, game_over with appropriate overlays
    - Auto-pause support when Claude needs input (loopAttention integration)
    - Dimension-responsive game board sizing
    - In-memory high score tracking within session
  - Updated `src-opentui/games/index.ts` - Added Snake to game registry
    - Imported SnakeGame and snakeGameInfo
    - Added to gameRegistry array
    - Re-exported for direct access
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 4.2: Adapt rendering to OpenTUI components (Pong)**
  - Created `src-opentui/games/PongGame.tsx` - Pong game for OpenTUI
    - Full game logic preserved from original (paddle movement, ball physics, AI opponent, scoring)
    - Rendering adapted to use OpenTUI `<box>` and `<text>` components
    - 2D character board rendering with colored elements (player paddle, AI paddle, ball, border, center line)
    - useKeyboard hook for input handling (Arrow keys, W/S, P/R/Q controls)
    - useGameLoop for frame-based timing with delta-time movement
    - Player paddle controlled with up/down keys, AI tracks ball with delay
    - Ball collision with paddles includes spin based on hit position
    - Ball speed increases slightly on each paddle hit
    - HUD showing player score, AI score, win condition, and FPS
    - Game states: playing, paused, game_over with appropriate overlays
    - Win/lose detection at 5 points (first to WIN_SCORE wins)
    - Auto-pause support when Claude needs input (loopAttention integration)
    - Dimension-responsive game board sizing
  - Updated `src-opentui/games/index.ts` - Added Pong to game registry
    - Imported PongGame and pongGameInfo
    - Added to gameRegistry array
    - Re-exported for direct access
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 4.2: Adapt rendering to OpenTUI components (Tetris)**
  - Created `src-opentui/games/TetrisGame.tsx` - Tetris game for OpenTUI
    - Full game logic preserved from original (7 standard pieces, rotation, wall kicks, line clearing, scoring)
    - Rendering adapted to use OpenTUI `<box>` and `<text>` components
    - 2D character board rendering with colored tetrominos (I, O, T, S, Z, J, L pieces)
    - useKeyboard hook for input handling (Arrow keys, WASD, Space for hard drop, P/R/Q controls)
    - useGameLoop for frame-based timing with level-based drop speed
    - Ghost piece preview showing where the current piece will land
    - Next piece preview panel showing upcoming tetromino
    - Line clearing animation with flash effect
    - Scoring system: 100 (1 line), 300 (2 lines), 500 (3 lines), 800 (Tetris)
    - Level system: every 10 lines cleared increases level, speeds up drop
    - HUD showing score, level, lines cleared, high score, and FPS
    - Game states: playing, paused, game_over with appropriate overlays
    - Auto-pause support when Claude needs input (loopAttention integration)
    - In-memory high score tracking within session
  - Updated `src-opentui/games/index.ts` - Added Tetris to game registry
    - Imported TetrisGame and tetrisGameInfo
    - Added to gameRegistry array
    - Re-exported for direct access
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

- [x] **Task 4.2: Adapt rendering to OpenTUI components (Minesweeper)**
  - Created `src-opentui/games/MinesweeperGame.tsx` - Minesweeper game for OpenTUI
    - Full game logic preserved from original (mine placement, reveal cascade, flag mechanics, chord reveal)
    - Rendering adapted to use OpenTUI `<box>` and `<text>` components
    - 2D character board rendering with colored elements (hidden cells, flags, mines, numbers)
    - useKeyboard hook for input handling (Arrow keys, WASD, Space to reveal, F to flag, D for difficulty)
    - Cursor-based navigation with visual highlight
    - Three difficulty levels: Easy (9x9, 10 mines), Medium (16x16, 40 mines), Hard (20x12, 60 mines)
    - Difficulty selector screen with arrow key navigation
    - Timer that starts after first click (safe first click guaranteed)
    - Mine counter showing remaining unflagged mines
    - Chord reveal: clicking revealed number when adjacent flags match reveals remaining neighbors
    - Colored numbers for adjacent mine counts (1-8)
    - Game states: playing, paused, won, lost, selecting_difficulty with appropriate screens
    - Win detection when all non-mine cells revealed
    - Best times tracking per difficulty (in-memory for session)
    - Auto-pause support when Claude needs input (loopAttention integration)
    - HUD showing mines remaining, time elapsed, difficulty, and best time
  - Updated `src-opentui/games/index.ts` - Added Minesweeper to game registry
    - Imported MinesweeperGame and minesweeperGameInfo
    - Added to gameRegistry array
    - Re-exported for direct access
    - All 4 games now ported: Snake, Pong, Tetris, Minesweeper
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

### Phase 5: Polish

- [x] **Task 5.1: Theme system**
  - Created `src-opentui/themes/theme.ts` - Theme constants and utilities
    - `hexColors` map converting terminal color names to hex values for OpenTUI
    - `colorToHex()` utility function for color conversion
    - Box drawing characters (light, heavy, double, rounded)
    - Status icons, alert icons, navigation icons
    - Progress indicators (filled, empty, spinners, braille animations)
    - Game-specific characters (snake, tetris, minesweeper symbols)
    - Decorative characters (stars, hearts, etc.)
    - Border styles and spacing constants
    - Utility functions: `getSpinnerFrame()`, `createProgressBar()`, `createDivider()`, `createBoxLine()`
    - Text utilities: `truncate()`, `centerText()`
  - Created `src-opentui/themes/themes.ts` - Theme definitions with hex colors
    - `Theme` interface with colors, statusColors, alertColors, gameColors, rankColors
    - 4 themes ported with hex color values:
      - `default` - Modern cyan/magenta/yellow terminal aesthetic
      - `dark` - Bright colors for dark backgrounds
      - `light` - High contrast for light terminal backgrounds
      - `retro` - Classic green phosphor terminal aesthetic
    - Theme registry functions: `getTheme()`, `getThemeIds()`, `getThemeInfo()`, `isValidThemeId()`
    - Color lookup functions: `getStatusColor()`, `getStatusIcon()`, `getAlertColor()`, `getRankColor()`
  - Created `src-opentui/themes/useTheme.tsx` - React context provider
    - `ThemeProvider` component with initial theme and color overrides support
    - `useTheme()` hook returning full context (theme, themeId, setTheme, toggleTheme, nextTheme)
    - Convenience hooks: `useThemeColors()`, `useStatusColors()`, `useAlertColors()`, `useGameColors()`, `useRankColors()`
    - Themed utility functions: `getThemedStatusColor()`, `getThemedAlertColor()`, `getThemedRankColor()`
  - Created `src-opentui/themes/index.ts` - Centralized exports
    - Exports all constants, types, functions from theme.ts
    - Exports all theme definitions, registry functions from themes.ts
    - Exports React context, provider, and all hooks from useTheme.tsx
  - Verified: `npm run typecheck` passes
  - Verified: `npm run build` passes

## Current Session

Started: 2026-01-15
